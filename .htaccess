RewriteEngine On
RewriteBase /PHP_project/
# Если это не реальный файл и не папка
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Перенаправляем всё на index.php
RewriteRule ^(.*)$ index.php [L,QSA]